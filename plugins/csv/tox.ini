[tox]
envlist =
    py{36,37}

[testenv]
deps = 
    pytest-console-scripts
# TODO look into setting conda_deps so that they are taken from environment.yml
conda_deps =
    pytest
    pandas
    xlrd
commands =
    pytest -v tests/
[pytest]
script_launch_mode = subprocess
