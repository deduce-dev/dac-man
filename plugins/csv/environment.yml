name: dacman-csv-test
dependencies:
  # TODO test with other python versions (tox)
  # this requires deciding what are the versions we want to support
  - python=3.6
  - pandas>=0.24
  # TODO add package for parsing Excel files
  - pip:
    # This depends on the current directory structure, and it assumes that:
    # 1) this plugin is two directories down from the root Dac-man repository
    # (currently: ${REPO_ROOT}/plugins/${NAME})
    # 2) Dac-man can be pip-installed with `pip install .` from the root directory
    - '../..'
    # This assumes that the plugin can be installed via `pip install .`
    - '.'
