# we need to have "requirements.txt" included in sdist,
# which in turn is required for tox to install the dependencies
include *.txt

# necessary to include package_data when creating sdist
graft dacman
